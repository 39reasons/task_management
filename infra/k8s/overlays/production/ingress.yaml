apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: task-management
  namespace: task-management
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/websocket-services: backend
spec:
  rules:
    - host: app.45.55.105.198.nip.io
      http:
        paths:
          - path: /graphql
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 4000
          - path: /realtime
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 4001
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
    - host: api.45.55.105.198.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 4000
          - path: /realtime
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 4001
    - host: app.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
    - host: api.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 4000
          - path: /realtime
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 4001
