type Comment {
  id: ID!
  task_id: ID!
  user_id: ID!
  content: String!
  created_at: String!
  updated_at: String!
  user: User!
}

extend type Task {
  comments: [Comment!]!
}

extend type Mutation {
  addComment(task_id: ID!, content: String!): Comment!
  deleteComment(id: ID!): Boolean!
}
